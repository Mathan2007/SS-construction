# TODO

- [ ] Update `index.html` meta tags (description, keywords/robots/OG tags) as requested.
- [ ] Ensure the existing `<title>` remains unchanged unless requested.
- [ ] Run `npm run build` (or `npm run dev` smoke check) to confirm no HTML/template issues.

