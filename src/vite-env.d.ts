/// <reference types="vite/client" />

// Allow importing static assets (images) in TSX/TS.
// Vite already provides these types, but keeping this file ensures compatibility
// across different TS/IDE configurations.

